interface RouteNames {
  LOGIN: string;
  REGISTER: string;
  HOME: string;
  PROFILE: string;
}

const ROUTES: RouteNames = {
  // Auth
  LOGIN: 'Login',
  REGISTER: 'Register',

  // Main
  HOME: 'Home',
  PROFILE: 'Profile',
};

export default ROUTES;
